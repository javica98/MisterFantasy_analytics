# Herramientas de desarrollo — no necesarias para ejecutar el pipeline en
# producción, solo para contribuir (lint, tests). Ver requirements.txt para
# las dependencias de runtime.
-r requirements.txt

ruff==0.16.4
pytest==9.0.3
